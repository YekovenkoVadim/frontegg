name: Playwright Tests
on:
  push:
    branches: [ master ]
jobs:
  playwrighttests:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    container: 
      image: mcr.microsoft.com/playwright:v1.40.1-focal
      env:
        BASEURL: ${{ secrets.BASEURL }}
        USERNAME: ${{ secrets.USERNAME }}
        PASSWORD: ${{ secrets.PASSWORD }}
        # BASEURL: https://app-cmo8zo3vh6q4.frontegg.com/oauth/account/login
        # USERNAME: vadim.yekovenko@gmail.com 
        # PASSWORD: PASSWORD=Qwerty1!
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Run tests
        run: |
          echo "Install dependencies from lockfile"
          npm ci
          echo "Run playwright tests"
          npm run e2e-ci
          # npx playwright test --grep @e2e --config ./playwright.config.ts --workers 3

          echo "Upload test results"
          if: always()
          uses: actions/upload-artifact@v3
          with:
            name: test-result-report
            path: reports/report.html

          # echo "Generate allure report"
          # - uses: actions/upload-artifact@master
          # with:
          #   name: allure-results
          #   path: allure-results
          #   retention-days: 10
          
          # echo "Get Allure history"
          # - uses: actions/checkout@v2
          #   if: always()
          #   continue-on-error: true
          #   with:
          #     ref: gh-pages
          #     path: gh-pages
          
          # echo "Test marketplace action"
          # - uses: simple-elf/allure-report-action@master
          #   if: always()
          #   id: allure-report
          #   with:
          #     allure_results: allure-results
          #     gh_pages: gh-pages
          #     allure_report: allure-report
          #     allure_history: allure-history
          #     keep_reports: 10
      
          # echo "Deploy report to Github Pages"
          # - name: Deploy report to Github Pages
          #   if: always()
          #   uses: peaceiris/actions-gh-pages@v2
          #   env:
          #     PERSONAL_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          #     PUBLISH_BRANCH: gh-pages
          #     PUBLISH_DIR: allure-history
